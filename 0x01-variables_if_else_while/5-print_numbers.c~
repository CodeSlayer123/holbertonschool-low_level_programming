#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - prints alphabet lowercase, but not q and e, using putchar
 *
 * Return: will return 0
 */
int main(void)
{
	int n;

	//	for (n = 0; n <= 9; n++)

	
	putchar(n);
	
	putchar('\n');
	return (0);
}

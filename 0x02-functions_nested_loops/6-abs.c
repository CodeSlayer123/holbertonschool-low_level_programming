#include "main.h"
/**
 * _abs - computes absolute value of integer
 *
 * @int: number
 *
 * Return: this is what's supposed to be returned to main
 */

	int _abs(int);
{
	int n;

	if (n < 0)
	{
	n = n - n - n;
	putchar(n);
	}

}

#include "main.h"
/**
 * rev_string - reverses string
 *
 * @s: string
 *
 * Return: Always 0
 */

void rev_string(char *s)
{
  
  
	while (*s != '\0')
	{
		s++;
	}
	s--;
	while (*s >= 0)
	{
	  char r[10];  

		r[10] = *s;
		s--;

		if (*s == 0)
		{
			break;
		}

	}
	*s = r;
	
}
	

